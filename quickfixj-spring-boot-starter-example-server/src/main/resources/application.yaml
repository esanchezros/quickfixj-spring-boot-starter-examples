quickfixj:
  server:
    config: classpath:quickfixj-server.cfg
    jmx-enabled: true

server:
  port: 8081

management:
  endpoint:
    quickfixjserver:
      enabled: true
  endpoints:
    web:
      exposure:
        include: quickfixjserver